export const name = 'omnimux-inspiration'
export const inject = []

/**
 * Host half is a load marker. Inspiration HTTP stays on the hub.
 * @param {{ effect?: (factory: () => () => void, label?: string) => void }} _ctx
 */
export function apply(_ctx) {}
