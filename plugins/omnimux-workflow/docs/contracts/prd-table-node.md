# 【PRD & 交互技术规范】画布结构化数据表节点（Canvas Table Node / `.htable`）

> **文档版本**：v1.1.0（基于最新确认的高保真交互原型全量修订）  
> **文档密级**：内部技术方案与开发实施 PRD  
> **适用范围**：DSH / OmniMux / MiniMax Design 类 AI 无限画布工作流的结构化数据表节点与全屏独立电子表格开发

---

## 1. 业务背景与设计定位

在 AI 多模态生成与无限画布（Canvas / Node DAG Workflow）交互场景中，用户不仅需要处理单点的文本、图片、音视频生成，更需要**批量化、结构化、多模态联动**的数据组织能力：
* **角色/资产库管理**：分镜剧本、人物设定、Prompt 模板、参考图/视频附件的结构化归档；
* **批量生产分发（Batch Dispatching）**：以表格每一行为输入，批量驱动下游文生图、图生视频、音频合成等生成节点；
* **人机协作界面**：既支持人类进行类似 Airtable / Excel 的精细化表格编辑与快速配置，又支持 AI Agent（通过 MCP/Tool）以结构化 JSON 进行高频自动读写。

为此，系统设计了**双态切换**的结构化数据表系统：在画布中作为 **轻量卡片节点** 进行 DAG 连线与快速预览；双击或点击全屏进入 **全屏独立电子表格（Spreadsheet Stage）** 进行沉浸式精细编辑。

---

## 2. 架构设计与存储规范

### 2.1 双层解耦存储模型（Dual-layer Storage）

为避免因庞大的表格数据（数千行或大量 Base64/Asset 引用）造成画布主索引文件 `canvas.json` 膨胀及渲染卡顿，采用**两层分离存储机制**：

```
工作区目录 / (Workspace Root)
├── .hilo/ (或 .omnimux/)
│   ├── canvas.json              # [L1 画布层] 记录节点几何位置、连接边、元数据索引
│   ├── tables/
│   │   ├── oyrvzzq6gyan.htable  # [L2 表体层] 独立的结构化数据表文件 (JSON 规范)
│   │   └── <tableId>.htable
│   ├── storage.json             # 工作区全局配置与偏好
│   └── index.sqlite             # 资产与版本索引数据库
```

```
┌────────────────────────────────────────────────────────┐
│  canvas.json (轻量节点索引)                             │
│  {                                                     │
│    "id": "node_tbl_01",                                │
│    "type": "table",                                    │
│    "data": { "path": ".hilo/tables/tbl_01.htable" }    │
│  }                                                     │
└──────────────────────────┬─────────────────────────────┘
                           │ 异步加载 / 引用解耦
                           ▼
┌────────────────────────────────────────────────────────┐
│  .hilo/tables/tbl_01.htable (完整表体 Payload)           │
│  { "version": 1, "columns": [...], "rows": [...] }     │
└────────────────────────────────────────────────────────┘
```

### 2.2 存储原子性与并发安全
* **路径命名约束**：工作区相对路径 `\.hilo\/tables\/[A-Za-z0-9_-]+\.htable$`；
* **原子写入**：采用 `tmp_file -> rename` 模式落地，杜绝并发写入时产生残缺 JSON；
* **文件锁/节点互斥锁**：后端提供基于 `nodeId / tablePath` 的读写互斥锁，避免前端编辑器与后台 Agent/MCP 工具同时覆写产生竞态。

---

## 3. 数据模型与 Schema 规范（`.htable`）

### 3.1 完整 Schema 定义（TypeScript 描述）

```typescript
/** 表格存储主格式 */
export interface HTableDocument {
  /** 格式版本号，当前固定为 1 */
  version: number;
  /** 表格标题名称 */
  title?: string;
  /** 字段列定义列表 */
  columns: HTableColumn[];
  /** 行数据列表 */
  rows: HTableRow[];
  /** 视图持久化筛选规则（可选） */
  filter?: HTableFilter;
  /** 行高显示模式（可选，默认 "low"） */
  rowHeight?: 'low' | 'medium' | 'tall' | 'extraTall';
}

/** 字段类型定义：文本 | 数字 | 附件 */
export type HTableFieldType = 'text' | 'number' | 'attachment';

/** 字段列定义 */
export interface HTableColumn {
  /** 列唯一标识 (如 col_xehuo4mi) */
  id: string;
  /** 列名 / 字段标题 */
  title: string;
  /** 字段类型 */
  type: HTableFieldType;
  /** 是否显示在表格视图中 */
  visible: boolean;
  /** 列宽（像素，范围 80 ~ 2000，默认 240） */
  width?: number;
}

/** 附件单元格结构 */
export interface HTableAttachment {
  /** 资产 ID */
  assetId: string;
  /** 资产文件名 */
  name: string;
  /** 资产多模态分类 */
  kind: 'image' | 'video' | 'audio' | 'text';
  /** 可选缩略图 URL / 相对路径 */
  thumbnailUrl?: string;
}

/** 单元格值类型：文本 | 数值 | 附件列表 | 空值 */
export type HTableCellValue = string | number | HTableAttachment[] | null;

/** 行记录定义 */
export interface HTableRow {
  /** 单元格数组，下标严格对应 columns[i] */
  cells: HTableCellValue[];
}

/** 筛选匹配策略 */
export type FilterMatchPolicy = 'all' | 'any'; // all: 且, any: 或

/** 筛选运算符 */
export type FilterOperator =
  | 'equals'      // 等于
  | 'notEquals'   // 不等于
  | 'contains'    // 包含
  | 'notContains' // 不包含
  | 'gt'          // 大于 (数值)
  | 'gte'         // 大于等于 (数值)
  | 'lt'          // 小于 (数值)
  | 'lte'         // 小于等于 (数值)
  | 'empty'       // 为空
  | 'notEmpty';   // 不为空

/** 单条筛选条件 */
export interface HTableFilterCondition {
  /** 目标列索引下标 (0-based) */
  columnIndex: number;
  /** 匹配运算符 */
  op: FilterOperator;
  /** 比较值 (empty / notEmpty 时可缺省) */
  value?: string | number;
}

/** 筛选器配置 */
export interface HTableFilter {
  match: FilterMatchPolicy;
  conditions: HTableFilterCondition[];
}
```

---

## 4. UI 界面与交互行为规格（PRD 详述）

系统包含 **视图 1：画布卡片态** 与 **视图 2：全屏独立电子表格态**，以及 **三大核心配置面板（Popovers）** 与 **【添加/编辑列】模态弹窗（Modal）**。

```
+-----------------------------------------------------------------------------------------+
| [视图 1] 画布缩略卡片态 (Canvas Preview Node)                                              |
| ┌─────────────────────────────────────────────────────────────┐                         |
| │ ▦ 短剧分镜角色表                             [+] [⛶ 全屏]   │                         |
| │ ┌─────────────────────────────────────────────────────────┐ │                         |
| │ │ A≡ 文本                                                  │ │                         |
| │ ├─────────────────────────────────────────────────────────┤ │          ( + )          |
| │ │ 1. 动态高速平行跟随镜头...                               │ │     DAG 批处理端口       |
| │ │ 2. 特写镜头：主角眼神坚定...                             │ │                         |
| │ └─────────────────────────────────────────────────────────┘ │                         |
| └─────────────────────────────────────────────────────────────┘                         |
+-----------------------------------------------------------------------------------------+
                                    │ 双击卡片 / 点击 [⛶ 全屏]
                                    ▼
+-----------------------------------------------------------------------------------------+
| [视图 2] 全屏独立电子表格态 (Full-screen Spreadsheet Stage)                               |
| ┌─────────────────────────────────────────────────────────────────────────────────────┐ │
| │ 未命名表格               [⚙ 字段配置] [▽ 筛选 ●] [三↕ 行高] | [↶] [↷] | [✕ 关闭]    │ │
| ├─────────────────────────────────────────────────────────────────────────────────────┤ │
| │ [ ] │ A≡ 文本                │ 123 数字          │ 📎 附件           │ + 添加列      │ │
| ├─────┼────────────────────────┼───────────────────┼───────────────────┼──────────────┤ │
| │  1  │ 提示词模板 A           │ 100               │ [封面.png]        │              │ │
| │  2  │ 提示词模板 B           │ 250               │ [参考.mp4]        │              │ │
| ├─────┴────────────────────────┴───────────────────┴───────────────────┴──────────────┤ │
| │ + 添加行                                                                            │ │
| └─────────────────────────────────────────────────────────────────────────────────────┘ │
+-----------------------------------------------------------------------------------------+
```

---

### 4.1 视图 1：画布缩略卡片态（Canvas Preview Node）

1. **外围标题与控制栏**：
   - 节点左上方外挂显示表格图标与名称（与全屏表名双向实时同步）；
   - 节点顶部中央悬浮操作条：`[+] 添加数据行`、`[⛶] 全屏独立编辑表格`；
   - 节点边框提供 4 处微调手柄（NW / NE / SW / SE）。
2. **卡片内部展示**：
   - 表头区域显示首字段类型与列名；
   - 内容体：渲染前 3 行预览数据及行号索引；超过 3 行时底部弱化显示 `... 共 N 条记录`；空表时显示引导语：*“暂无数据 — 点击下方 + 添加一行”*；
   - 双击卡片主体任意区域即触发进入全屏电子表格模式。
3. **右侧 DAG 批处理端口 (`+`)**：
   - 提供向外连线端口，支持连接至下游文生图、图生视频、大模型节点，作为批处理数据集驱动。

---

### 4.2 视图 2：全屏独立电子表格态（Spreadsheet Stage）

1. **顶部导航工具条（Topbar）**：
   - **左侧标题区**：
     - 单一内联可编辑表名输入框（`未命名表格`，字号 16px 粗体），无多余切换控件，悬浮/聚焦呈现编辑外框，回车或失焦即时保存。
   - **右侧功能操作群**（胶囊风格按钮 `#f2f3f5`，高度 32px，圆角 8px）：
     - **`[⚙ 字段配置]`**：齿轮矢量图标 + 文案；点击直接在其正下方展开【字段配置】弹层；
     - **`[▽ 筛选]`**：漏斗矢量图标 + 文案；当表格存在生效的筛选规则时，按钮右上角显示蓝色实心徽标小圆点（`●`）；点击在其正下方展开【设置筛选条件】弹层；
     - **`[三↕ 行高]`**：三横线加双向微调箭头图标 + 文案；点击在其正下方展开【行高】预设切换弹层；
     - **分隔线**：1px 浅灰色竖线；
     - **`[↶ 撤销]`**：Undo（单表历史栈，支持 `Cmd+Z`）；
     - **`[↷ 重做]`**：Redo（单表历史栈，支持 `Cmd+Shift+Z`）；
     - **分隔线**：1px 浅灰色竖线；
     - **`[✕ 关闭]`**：退出全屏返回画布（支持 `Esc` 键）。

2. **数据表格网格区（DataGrid）**：
   - **固定首列**：44px 行选择与序号列，包含表头全选 Checkbox 及每行行号；
   - **动态字段列**：表头包含字段类型图标（`A≡` 文本、`123` 数字、`📎` 附件）+ 字段标题；支持拖拽调整列宽；
   - **快捷加列列**：表头最右侧固定为 50px 的 `+` 列，点击直接呼出【添加列】弹窗；
   - **单元格内联编辑**：纯文本/数字单元格支持聚焦内联输入，附件单元格支持点击资产弹窗与拖拽文件；
   - **底部动作条**：表格下方常驻 `+ 添加行` 按钮。

---

### 4.3 三大核心配置面板（Popovers）规格

每个面板均采用**按钮正下方精准锚定（Anchor Positioning）**与独立事件隔离（`event.stopPropagation()`），点击外部空白区域自动收起。

#### 1. 【字段配置】面板（Field Configuration Popover）
* **定位**：锚定于 `[⚙ 字段配置]` 按钮正下方，宽度 320px，圆角 16px，带悬浮投影；
* **标题**：顶部固定标题 “字段配置”；
* **字段卡片列表**（垂直列表，最大高度 360px 可滚动）：
  * 每行对应一个字段，背景为 `#f2f3f5` 浅灰圆角卡片；
  * **左侧**：6 触点拖拽把手（`::`，可拖拽调整字段显示次序） + 字段类型矢量图标 + 字段名称；
  * **右侧**：
    * **眼睛图标按钮**：点击切换列可见性（`visible: true/false`）；隐藏态图标为划线眼睛且颜色置灰；
    * **更多操作按钮（`···`）**：点击在光标旁弹出二级操作气泡：
      * `[✏ 编辑]`：唤起【编辑列】弹窗（带入现有字段名与字段类型）；
      * `[🗑 删除]`：红色危险项，点击确认后删除该列并级联剔除所有行的对应列数据；
* **底部固定入口**：`+ 新增字段` 按钮（点击呼出【添加列】弹窗）。

#### 2. 【设置筛选条件】面板（Filter Builder Popover）
* **定位**：锚定于 `[▽ 筛选]` 按钮正下方，宽度 480px，圆角 16px；
* **标题**：顶部固定标题 “设置筛选条件”；
* **条件行组件（横向排列）**：
  1. **字段选择胶囊**：宽度 110px，显示当前选中列名 + 下拉箭头；
  2. **运算符选择胶囊**：宽度 110px，显示当前运算符（如 `等于`）+ 下拉箭头；点击展开运算符气泡菜单（包含：`等于`、`不等于`、`包含`、`不包含`、`为空`、`不为空`，当前选中项右侧带黑色对勾 `✓`）；
  3. **比较值输入胶囊**：占满剩余宽度，文本输入框（placeholder: `请输入`）；
  4. **删除按钮**：右侧圆形 `✕` 按钮，点击删除该条过滤规则；
* **底部操作**：`+ 添加条件` 按钮，支持多条件叠加；
* **联动效果**：存在至少一条生效条件时，顶部工具栏 `[▽ 筛选]` 按钮右上角展示蓝色圆点徽标（Blue Badge Dot）。

#### 3. 【行高】预设面板（Row Height Popover）
* **定位**：锚定于 `[三↕ 行高]` 按钮正下方，宽度 180px，圆角 16px；
* **标题**：顶部固定标题 “行高”；
* **4 档预设选项**（每项右侧选中态展示黑色对勾 `✓`）：
  1. **低 (`low`)**：行高 34px（紧凑型纯文本与密集数据排查）；
  2. **中等 (`medium`)**：行高 48px（标准行高，适合单行文本与标签）；
  3. **高 (`tall`)**：行高 72px（舒适行高，适合多行文本与小型附件缩略图）；
  4. **超高 (`extraTall`)**：行高 120px（大卡片行高，适合多模态图片、视频分镜大图卡片预览）。

---

### 4.4 【添加列 / 编辑列】模态弹窗（Column Modal）规格

* **展示形式**：居中模态遮罩（Modal Backdrop，带半透明模糊），卡片宽度 440px，圆角 20px；
* **标题**：根据触发模式动态显示 “添加列” 或 “编辑列”，右上角带关闭按钮 `✕`；
* **表单内容**：
  1. **字段名**：输入框（高度 42px，圆角 12px，placeholder: `请输入字段名`），打开时自动获取焦点；
  2. **字段类型选择器**：自定义下拉选择触发器（高度 42px，圆角 12px），点击展开 3 种标准支持类型列表：
     * `文本`（Text）- 选中态右侧显示对勾 `✓`；
     * `数字`（Number）- 选中态右侧显示对勾 `✓`；
     * `附件`（Attachment）- 选中态右侧显示对勾 `✓`；
* **底部操作栏**：
  * `[取消]`：浅灰按钮；
  * `[确定]`：品牌蓝主色按钮（`#165dff`），支持回车键快捷提交。

---

## 5. 多模态附件与 DAG 批量衍生协同

### 5.1 附件单元格设计（Multi-modal Attachment）
* **数据结构**：单元格存储为 `HTableAttachment[]` 数组。
* **交互行为**：
  * 支持从本地文件管理器拖拽图片/视频/音频直接放入单元格；
  * 支持点击弹出多模态资产选择器（结合工作区资产库）；
  * 支持右键将上游生成节点（图片/视频）“写入表格指定单元格”。

### 5.2 表格驱动的 DAG 批处理机制（Batch Derivation）
* 当从表格节点的右侧端口连线到下游任务节点时：
  * **参数映射**：下游节点可绑定表格字段作为输入变量（如 `Prompt = {{row.分镜描述}}`，`ReferenceImage = {{row.角色参考图}}`）；
  * **并发控制**：工作区执行器（DAG Executor）根据表格的有效行列表进行并行或串行迭代调度，并将生成的产物资产写回下游节点或生成新的资产行。

---

## 6. AI Agent / MCP 工具协议标准

系统需暴露标准 MCP Tools，让 AI Agent 可以直接读写表格：

### 6.1 `canvas_write_table_node`（创建 / 覆写表格节点）
* **入参格式（Input Schema）**：
  ```json
  {
    "nodeId": "optional string (有值则更新现有表格，缺省则新建节点)",
    "title": "表格名称 (如: '短剧分镜表')",
    "columns": [
      { "title": "场次", "type": "number", "width": 80 },
      { "title": "分镜描述", "type": "text", "width": 300 },
      { "title": "参考图", "type": "attachment", "width": 150 }
    ],
    "rows": [
      {
        "cells": [
          1,
          "动态高速平行跟随镜头。非洲大草原上猎豹冲刺",
          [{ "assetId": "ast_123", "name": "ref.png", "kind": "image" }]
        ]
      }
    ],
    "filter": {
      "match": "all",
      "conditions": []
    },
    "rowHeight": "tall",
    "position": { "x": 100, "y": 200 },
    "sourceNodeIds": ["node_upstream_script"]
  }
  ```
* **出参格式（Output Schema）**：
  ```json
  {
    "nodeId": "node_tbl_xxxx",
    "tablePath": ".hilo/tables/xxxx.htable",
    "columnCount": 3,
    "rowCount": 1,
    "created": true
  }
  ```

### 6.2 `canvas_get_node`（读取表格节点详情）
* 对 Agent 返回清洗后的 `tableContent`（包含 `columns`、`rows`、`filter`、`rowHeight`），屏蔽底层的物理存储逻辑，方便 LLM 快速结构化理解和二次编辑。

---

## 7. 前端实现技术选型建议

| 模块 | 推荐选型 | 理由与考量 |
|:---|:---|:---|
| **表格渲染引擎** | `@tanstack/react-table` + 虚拟滚动 (`@tanstack/react-virtual`) | 无头（Headless）架构，完全解耦 UI，能 100% 还原自定义的主题 Token 与圆角卡片视觉；支持超大表性能。 |
| **画布节点集成** | `@xyflow/react` (React Flow 12) 自定义节点 | 标准节点包装器，将卡片态注册到 `nodeTypes.table`，与工作区 DAG 连线无缝打通。 |
| **弹层与下拉交互** | `@radix-ui/react-popover` / `@radix-ui/react-dialog` | 提供无障碍键盘支持、精准锚定定位与焦点陷阱控制。 |
| **状态与历史管理** | `zustand` + `immer` + 自建 Command History Stack | 保证全屏编辑态与画布卡片态的状态一致性，实现轻量可靠的单元格级 Undo/Redo。 |
| **持久化存储** | NodeJS / Electron `fs/promises` 原子写入 | 写入临时文件后重命名，配合互斥锁保障本地文件一致性。 |

---

## 8. 里程碑交付计划

* **M1（核心存储与 Schema 验证）**：实现 `.htable` 的 Zod Schema 校验、Node 读写互斥锁与底层原子读写 API。
* **M2（画布卡片节点实现）**：实现 React Flow `TableNode` 卡片视图、空状态引导、前 3 行数据预览渲染与 DAG 衍生端口。
* **M3（全屏沉浸式电子表格与弹层）**：交付全屏 Stage Overlay，完成字段配置（排序/显隐/新增/删除）、条件筛选器（All/Any、多运算符）、行高预设器、Undo/Redo、添加/编辑列模态弹窗。
* **M4（多模态附件与 Agent MCP 工具接通）**：支持单元格拖拽绑定多模态图片/视频资产，上线 `canvas_write_table_node` 与 `canvas_get_node` MCP 工具链路。
