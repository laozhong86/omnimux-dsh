# both schemaVersion + legacy version → fail closed (even if values "look equal")
schemaVersion: "1.1"
version: "1.1"
managementGroup: "text"
models:
  - id: "fixture-both-version"
    label: "Fixture Both Version"
    operations:
      - id: "chat"
        label: "chat"
        output:
          type: "text"
        inputs:
          - slot: "prompt"
            type: "text"
            role: "prompt"
            source: "node_field"
            min: 1
            max: 1
        research:
          status: "draft"
        execution:
          status: "none"
