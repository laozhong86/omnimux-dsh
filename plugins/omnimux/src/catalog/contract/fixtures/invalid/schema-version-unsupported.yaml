# schemaVersion present but not exact "1.1"
schemaVersion: "1.0"
managementGroup: "text"
models:
  - id: "fixture-unsupported-schema-version"
    label: "Fixture Unsupported Schema Version"
    operations:
      - id: "chat"
        label: "chat"
        output:
          type: "text"
        inputs:
          - slot: "prompt"
            type: "text"
            role: "prompt"
            source: "node_field"
            min: 1
            max: 1
        research:
          status: "draft"
        execution:
          status: "none"
