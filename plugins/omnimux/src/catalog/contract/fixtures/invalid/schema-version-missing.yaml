# missing schemaVersion and missing legacy version
managementGroup: "text"
models:
  - id: "fixture-missing-schema-version"
    label: "Fixture Missing Schema Version"
    operations:
      - id: "chat"
        label: "chat"
        output:
          type: "text"
        inputs:
          - slot: "prompt"
            type: "text"
            role: "prompt"
            source: "node_field"
            min: 1
            max: 1
        research:
          status: "draft"
        execution:
          status: "none"
