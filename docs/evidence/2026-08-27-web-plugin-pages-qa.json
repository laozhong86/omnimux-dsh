{
  "date": "2026-08-27",
  "task": "web plugin pages QA — isolated worktree omnimux-dsh-wt-plugin-pages (branch agent/web-qa-plugin-pages)",
  "service": {
    "entry": "fork `yarn omnimux:dev start plugin-pages omnimux`",
    "url": "http://127.0.0.1:44200",
    "home": "~/.dsh-dev/tasks/plugin-pages",
    "profile": "omnimux-dev-plugin-pages",
    "note": "one linked worktree plugin per profile rule honored; other plugins are materialized stable copies from production profile"
  },
  "rootCausesAndFixes": [
    {
      "symptom": "Active first-level page root rendered but every direct child (header/action-row/filter/body) computed display:none with zero rects on Accounts/Analytics/Workflow/Assets/Products/Inspiration",
      "rootCause": "plugins/omnimux/src/client/conversation-box.js STAGE_MUTUAL_EXCLUSION_RULES used descendant-wide selector html[data-dsh-product-stage=\"ID\"] [class*=\"-stage\"]:not(.ACTIVE) which also matched the active stage's own header/body classes containing the '-stage' fragment",
      "fix": "scope each exclusion rule to sibling roots: html[data-dsh-product-stage=\"ID\"] [data-slot=\"shell.overlay\"] > [class*=\"-stage\"]:not(.ACTIVE)",
      "regressionGuard": "plugins/omnimux/src/client/stage-mutual-exclusion.test.js — new 'scopes mutual exclusion to sibling stage roots, not descendants' case plus updated expected-rule assertions (red before fix, green after)",
      "siblingSweep": "grep for [class*=\"-stage\"] shaped rules across plugins: only conversation-box.js generated this pattern; market/plaza, clip, publish use their own class names and do not match '-stage' fragment"
    },
    {
      "symptom": "L2 watcher died at startup: watch.log '/private/var/folders/.../xfs-71cffaf6/node: No such file or directory'; dev promise of auto rebuild/HMR silently false",
      "rootCause": "scripts/dev-env.sh spawned background Host/watch children with bare `node` under Yarn Berry, whose xfs bin dir (first PATH entry + npm_node_execpath/npm_execpath) is deleted after the foreground command exits; detached children resolved a stale node path",
      "fix": "scripts/dev-env.sh resolve durable OMNIMUX_NODE_BIN / NVM_BIN / non-xfs command -v node at startup, strip */xfs-* entries from PATH for children, unset Yarn-only env (npm_execpath, npm_node_execpath, BERRY_BIN_FOLDER), and spawn all three nohup children (start watcher, start host, restart-host) with \"$NODE_BIN\"",
      "verification": "watcher pid alive as /Users/x/.nvm/versions/node/v25.8.0/bin/node .../omnimux-dsh-wt-plugin-pages/scripts/watch-plugin.mjs omnimux; watch.log shows real rebuild writing the worktree bundle and Host HMR push path intact",
      "note": "first post-fix restart bound the wrong ROOT because I omitted the fork env vars when invoking `watch`; corrected by unwatch+watch with explicit OMNIMUX_PRODUCT_DIR/OMNIMUX_PLUGINS_DIR — process evidence confirms the worktree script path"
    }
  ],
  "pageLedger": [
    {"page": "Accounts (omnimux-accounts)", "result": "pass", "evidence": "header visible; signed-in state lists 1 account (GeminiX TikTok Active), overview bar 4 metric cards; no hidden children"},
    {"page": "Projects / Workflow (omnimux-workflow)", "result": "pass", "evidence": "header+action row+toolbar+project list all visible"},
    {"page": "Publish (dsh-publish)", "result": "pass", "evidence": "header/action row/control bar/viewport visible; empty-state copy renders"},
    {"page": "Analytics (omnimux-analytics)", "result": "pass", "evidence": "claim->loading skeleton->ready dashboard ~3s; KPI grid and charts render; single legitimate claim event in instrumented dispatch trace; dataset stable whole run"},
    {"page": "Assets (omnimux-assets)", "result": "pass", "evidence": "header/action toolbar/body visible with add-asset flows"},
    {"page": "Inspiration (omnimux-inspiration)", "result": "pass", "evidence": "header + body chips render"},
    {"page": "Products (omnimux-products)", "result": "pass", "evidence": "header/toolbar/body visible; product library empty state"},
    {"page": "Extension Market plaza (omnimux-market)", "result": "pass", "evidence": "page rect {280,0,1624x913}; Plugins/Skills/Experts/Connectors tabs each render catalog lists; 48 cards detected on Plugins tab"},
    {"page": "Clip direct route ?clip=1&project=...", "result": "pass", "evidence": "__omnimuxClipReady=true; overlay flex full viewport 1904x913; timeline UI present"},
    {"page": "Settings dialog incl. hub surfaces", "result": "pass", "evidence": "opens with General/Profile/Models/Plugins/Agent presets/Subscriptions/Side card sections; zero data-slot-error"}
  ],
  "notPagesClassified": [
    {"surface": "omnimux-video", "classification": "host-only package (no dsh.client section) — nothing to load in browser"},
    {"surface": "omnimux-drama", "classification": "domain-only vertical (series domain + drama tools) — nothing to load in browser"},
    {"surface": "omnimux-video-preview client", "classification": "betterSidebar file-viewer registration (activates inside a session file viewer); stream route is a host API — not a standalone page"},
    {"surface": "/api/desktop/updates/status 404", "classification": "desktop-updater endpoint absent in web L2 host; unrelated to plugin pages, left untouched"}
  ],
  "verificationCommands": [
    "pnpm -s build:all            -> exit 0",
    "pnpm -s test                 -> exit 0 (953 tests, 0 failures across 7 suites)",
    "pnpm -s verify:stages        -> exit 0 ('All 9 Stage components satisfy architectural contracts perfectly.')",
    "node scripts/auto-qa-gate.mjs -> exit 0 (5D QA gate PASS)",
    "node scripts/verify-plugin-boundaries.mjs -> exit 0 (753 source files verified)",
    "ego-browser final sweep over 7 sidebar stages + market + clip route -> all rootVisible, hiddenKids=[], slotErrors=0"
  ],
  "processNotes": [
    "Earlier transient 'stage hidden immediately after open' observations were traced to my own probe scripts using an unanchored /continue|继续/i button matcher that clicked an unintended control; instrumented clean runs show correct lifecycle. Recorded to prevent recurrence.",
    "First build:all rerun failed because I invoked corepack pnpm (v11.8.0) against the repo's pnpm@11.7.0 packageManager gate; AGENTS.md mandates plain `pnpm <script>` which passes. Local discipline note only.",
    "Out-of-scope rebuilt bundles: `pnpm -s build:all` regenerated plugins/omnimux-analytics/lib/client.js and plugins/omnimux-video-preview/lib/client.js differently from their committed versions (upstream bundle drift). These were restored via git checkout to keep this change set scoped to the two fixes."
  ],
  "unresolved": [],
  "cleanupState": "this file written while L2 services still running; stop/closure recorded in final report"
}
