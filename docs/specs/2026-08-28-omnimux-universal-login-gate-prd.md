---
title: "OmniMux 全局登录态检查与通用授权弹窗 - 功能需求规格说明书 (PRD)"
id: "prd-omnimux-universal-login-gate"
type: "prd"
status: "accepted"
authority: "L2"
date: "2026-08-28"
updated: "2026-08-29"
authors: ["齐活林", "许清楚", "高见远"]
subsystem: "omnimux"
version: "1.1.0"
related:
  - "docs/specs/2026-08-20-omnimux-login-gate.md"
  - "docs/prototypes/omnimux-login-gate-modal.html"
  - "docs/contracts/hub.md"
  - "docs/contracts/ui-copywriting-and-naming-standards.md"
  - "plugins/omnimux/assets/logo.svg"
---

# OmniMux 全局登录态检查与通用授权弹窗 — 功能需求规格说明书 (PRD)

- **产品总监 / 编排**：齐活林
- **需求分析 / 定界**：许清楚
- **架构顾问**：高见远
- **原型基准**：`docs/prototypes/omnimux-login-gate-modal.html`（1:1 高保真交互原型 = 视觉真源）
- **架构对照**：`docs/specs/2026-08-20-omnimux-login-gate.md`（Seam / 状态机 / 意图恢复）
- **适用范围**：OmniMux 插件矩阵全量 Stage 一级独立页（工作流、资产库、产品库、剪辑工坊、灵感库、账号分发等）与跨插件能力调用。
- **文案锁定（老板拍板 2026-08-29）**：**「商业出海与出片」** 为唯一官方文案定位。版本 B / C / MiniMax 原版仅作探索对照，**禁止进入生产代码**。

---

## 0. 本轮审计结论（v1.1.0 Lock）

| 审计项 | 修订前残留 | 锁定结论 |
|---|---|---|
| 文案定位 | 版本 A / B / C 并列「推荐」 | **仅版本 A「商业出海与出片」为官方文案** |
| 主标题 | 场景 1 仍写「全面接入 AI Agent 矩阵」 | 统一为 **`OmniMux 全面接入商业 Agent 矩阵`** |
| 副标题 | ASCII 框图仍写「最强多模态生成管线」 | 统一为 **`最强多模态创作引擎 + 顶尖社媒运营专家团队`** |
| 特性三条 | 框图仍写旧稿「跨场景 / Agent Skill / 一路落地」 | 统一为下方 §4.2 三条锁定句 |
| 左侧海报排版 | 4 套预设并列 | **方案 1「黄金比例精修」为生产默认**；其余仅原型调试对照 |
| 徽标 | 已替换为官方章鱼 IP | **锁定**：`plugins/omnimux/assets/logo.svg`（`#C6F14F` + `#101B2B`） |
| 配图 | 用户确认保留深海鱼群/水母光晕 | **锁定**，禁止再换素材 |
| 文档状态 | `in-review` / 待审 | **`accepted`**，后续工程以本 PRD + 原型为双真源 |

---

## 1. 业务背景与产品定位

### 1.1 现状与痛点
1. **分散鉴权体验割裂**：目前 OmniMux 各垂直插件（如 `omnimux-accounts`、`omnimux-workflow` 等）对登录状态的检查缺乏统一的拦截视效与交互规范，部分场景出现未登录点击白屏、控制台抛错或弱提示。
2. **意图丢失（Context Drop）**：用户在未登录状态下点击某个具体业务插件（例如想打开「剪辑工坊」处理视频），跳往个人中心登录后，无法自动恢复并打开刚才被拦截的目标功能。
3. **品牌认知与转化不足**：缺乏高辨识度、有商业价值感的登录引导弹窗，难以向用户有效传达 OmniMux「商业出海与出片」的全链路价值（灵感 → 资产 → 画布 → 剪辑 → 多账号矩阵分发）。

### 1.2 产品定位与核心价值
- **统一中枢门禁（Hub-owned Gate）**：登录状态与授权弹窗作为 OmniMux 执行中枢（`plugins/omnimux`）的核心基础设施，由中枢统一提供 `window.__omnimuxAuth.ensureLogin()` Seam，垂直插件无需重复开发登录 UI。
- **免除冷启动门禁（No Cold-Start Block）**：保留用户「未登录可进 App、可逛货架与基础浏览」的低门槛体验；但在**点击/进入任意具体 OmniMux 插件功能**时，触发统一弹窗拦截。
- **意图无缝恢复（Resume-After-Login）**：登录成功后，系统自动清空阻断、拉起并打开用户最初点击的插件页面或重试中断动作。
- **商业出海与出片（Locked Positioning）**：弹窗不是「通用 AI 工具登录页」，而是把 OmniMux 定位为**商业内容出海的多模态 Agent 矩阵**——理解拆解多平台生产任务、用工作流画布与 Skill 十倍出片、从一条灵感陪用户全链路落地。

---

## 2. 产品目标 (Product Goals)

| 目标维度 | 目标定义与量化指标 |
|---|---|
| **G1: 体验统一性** | OmniMux 系列 100% 的一级 Stage 插件统一接入中枢登录门，未登录点击拦截率 100%，无白屏/野指针报错。 |
| **G2: 意图无缝恢复** | 登录完成后，原始中断动作（打开 Stage / 提交表单 / 发起生成）自动恢复成功率 ≥ 99.5%。 |
| **G3: 视觉与转化率** | 1:1 落地高保真弹窗（深海光晕海报 + 黄金比例排版 + 官方章鱼 IP + 锁定文案），登录引导转化率（CTR）相对现状弱提示提升 ≥ 35%。 |

---

## 3. 用户故事与典型场景

### 场景 1：未登录点击侧边栏一级 Stage 插件（主路径）
- **As a** 首次打开或未登录 OmniMux 桌面端的创作者 / 出海运营；
- **When** 在左侧侧边栏点击「工作流画布」或「剪辑工坊」；
- **Then** 系统不直接进入空白或报错页，而是立即平滑弹出「OmniMux 全面接入商业 Agent 矩阵」高保真弹窗，背景高斯模糊；
- **And** 用户点击「立即登录」完成设备码授权后，弹窗自动关闭，并自动切换并打开刚才点击的插件页面。

### 场景 2：浏览过程中登录态过期 / Token 失效
- **As a** 正在使用灵感库或产品库的用户；
- **When** 后端接口返回 401 `needs-omnimux` 认证失效时；
- **Then** 前端 API 封装层自动捕获，静默挂起当前重试请求，唤起通用授权弹窗；
- **And** 用户重新授权成功后，自动重发该请求，无需用户手动刷新页面。

### 场景 3：用户主动取消 / 关闭弹窗
- **As a** 仅想随意看看界面的游客用户；
- **When** 弹窗弹出后点击右上角「✕」或遮罩空白处或按 `ESC` 键；
- **Then** 弹窗顺滑收起，用户停留在当前主界面，系统清空挂起的待恢复意图队列，并给出轻量 Toast 提示。

---

## 4. UI 视觉规范与 1:1 复刻设计要求

高保真原型已固化于 `docs/prototypes/omnimux-login-gate-modal.html`。**生产落地必须以原型视觉 + 本节锁定规格为准**；实际组件必须消费 DSH 原生 `--dsw-*` Token，禁止把原型里的演示 hex 直接写进生产组件。

```
+-----------------------------------------------------------------------------------+
|  OmniMux 模态弹窗 (820px × 520px, Radius 20px, Deep Dark #161618, Shadow 64px)    |
| +--------------------------------+----------------------------------------------+ |
| | 左侧海报区 (348px, 42%)        | 右侧内容与操作区 (472px, 58%)                | |
| | - 深海鱼群/水母光晕背景(锁定)  | - 官方章鱼 IP Logo + OmniMux Studio          | |
| | - 底部高斯渐变遮罩 (Scrim)     | - 主标题: OmniMux 全面接入商业 Agent 矩阵     | |
| | - 黄金比例排版 (方案 1 锁定):  | - 副标题: 最强多模态创作引擎 + 顶尖社媒运营专家团队 | |
| |     • STUDIO SUITE (11px Tag)  | - 核心特性列表 (实心小方块 Bullet Cube):      | |
| |     OmniMux (32px Bold Sans)   |     ■ 理解、拆解并全自动执行多平台商业内容生产 | |
| |     AI (72px Serif 渐变艺术字) |     ■ 专业工作流画布与 Skill，释放十倍出片生产力 | |
| |                                |     ■ 从一个创意灵感开始，OmniMux 陪你全链路出海落地 | |
| |                                | - 底部操作区 (右对齐):                       | |
| |                                |     [ 立即登录 ] (白色高亮按钮, 40px 高)     | |
| +--------------------------------+----------------------------------------------+ |
|                                                       [右上角关闭 ✕ (32×32px)]   |
+-----------------------------------------------------------------------------------+
```

### 4.1 容器与左侧海报（视觉锁定）

1. **外层弹窗容器**：
   - 尺寸：宽 `820px`，高 `520px`（小于 860px 屏幕自动转为上下流式卡片）。
   - 圆角：`20px`（符合模态大弹窗规范）。
   - 背景色：`#161618`（生产映射 `--dsw-alias-surface-raised` / 深色底）。
   - 阴影：`0 24px 64px -12px rgba(0, 0, 0, 0.75)` + 紫色环境微光 `rgba(120, 40, 200, 0.18)`。
   - 边框：`1px solid rgba(255, 255, 255, 0.1)`。
2. **左侧海报区**：
   - 宽度：`348px`（占比约 42%）。
   - 配图素材：**已锁定**用户认可的海洋/鱼群/水母光晕深海素材（`mix-blend-mode: overlay` + 浮动微动效 `floatAura`）。禁止再换图。
   - 底部暗色遮罩衬底（Scrim）：高度 55%，`linear-gradient(to top, rgba(20, 10, 45, 0.85) 0%, rgba(20, 10, 45, 0.4) 55%, transparent 100%)`。
   - **排版锁定（方案 1 · 黄金比例精修）**：
     - 顶部标签：`STUDIO SUITE`（11px，字距 +2.5px，带紫色发光圆点）
     - 品牌字：`OmniMux`（32px，`Plus Jakarta Sans` / `Inter`，字重 800，字距 -0.8px）
     - 核心主字：`AI`（72px，`Cinzel` / `Playfair Display`，字距 -1.5px，双层渐变发光）
     - 方案 2 / 3 仅保留在原型调试栏，**生产代码不得实现切换器**。

### 4.2 右侧内容区与官方锁定文案（Copy Lock）

> **本小节是文案真源。** 工程、QA、后续修订一律以本表为准。禁止回退到「AI Agent 矩阵 / 生成管线 / 一路落地」等探索稿。

| 槽位 | 锁定文案 | 字号 / 样式 | 业务映射 |
|---|---|---|---|
| 品名徽标 | **OmniMux Studio** | 22px Serif | 官方章鱼 IP + 荧光黄绿虚线外框 |
| 主标题 | **OmniMux 全面接入商业 Agent 矩阵** | 22px Bold `#fff` | 多智能体专家协同（需求/架构/工程/QA/运营）服务商业内容生产 |
| 副标题 | **最强多模态创作引擎 + 顶尖社媒运营专家团队** | 14.5px Medium `#a1a1aa` | 音视频图文中枢 + 社媒出海垂直方案 |
| 特性 1 | **理解、拆解并全自动执行多平台商业内容生产** | 14.5px / Cube Bullet | TikTok / YouTube / 小红书等平台任务拆解与生成调度 |
| 特性 2 | **专业工作流画布与 Skill，释放十倍出片生产力** | 14.5px / Cube Bullet | `omnimux-workflow` 无限画布 + Skill 扩展 |
| 特性 3 | **从一个创意灵感开始，OmniMux 陪你全链路出海落地** | 14.5px / Cube Bullet | 灵感 → 资产 → 画布 → 剪辑 → 多账号矩阵分发 |
| CTA | **立即登录** | 40px 高，白底黑字，圆角 8px | 拉起设备码授权；轮询中展示 `OMX-XXXX` |

**徽标视觉规格（锁定）**：
- 尺寸：`32×32px`，圆角 `8px`。
- 素材：官方 `plugins/omnimux/assets/logo.svg`（C2-lively-v2 章鱼 IP，身体 `#C6F14F`，眼睛/触角 `#232A35`，底 `#101B2B`）。
- 外框：外扩 `3px` 的 `1px dashed rgba(198, 241, 79, 0.45)` 虚线点阵边框。
- 禁止使用图层/堆叠占位 icon，禁止使用 MiniMax 参考图里的角色头像。

**特性清单视觉规格**：
- 实心正方形灰色 Bullet：`6×6px`，`#71717a`，`margin-top: 7px`。
- 行高 1.45，条目间距 16px。

**关闭按钮**：
- 位置：右上角 `top: 18px; right: 18px;`。
- 尺寸：`32×32px`，圆角 `8px`，深色半透明微底，悬浮放大 1.06 倍。
- 支持点击 ✕、点击遮罩、`ESC` 三种关闭路径。

### 4.3 文案风格铁律（生产必须遵守）

1. **句式对齐参考图，语义对齐 OmniMux**：参考 MiniMax 的「全面接入 / 最强 A + 顶尖 B / 理解拆解执行 / 工作流与 Skill / 从一句想法开始陪你落地」五段骨架，但用词必须落到「商业 / 多平台 / 出片 / 出海 / 工作流画布」。
2. **禁止空泛 AI 黑话**：生产文案不得使用「生成管线」「多智能体协同生产力」「AI 创作中枢」等未锁定探索稿。
3. **CTA 动词纯粹**：主按钮只用 `立即登录`（对齐 `docs/contracts/ui-copywriting-and-naming-standards.md` 动词纯粹律）。轮询态用陈述句「已在新窗口打开授权页，请输入设备码：」，不用「请您尽快完成授权」客服腔。
4. **品牌写法**：品名锁定 `OmniMux Studio`；左侧海报品牌字锁定 `OmniMux`；禁止写成 `OmniMux AI Studio`、`Omnimux`、`OMNI MUX`。

---

## 5. 核心交互流程与状态机

### 5.1 交互时序图
```
[ 用户点击任意插件 ]
        │
        ▼
[ 触发 window.__omnimuxAuth.ensureLogin({ reason, onSuccess }) ]
        │
   [ 是否已登录? ]
     ├── 是 ──> 直接执行 onSuccess() (无弹窗，零感知进入)
     └── 否 ──> 挂起 intent，将弹窗状态置为 starting -> waiting
                  │
                  ▼
          [ 居中弹出 1:1 授权弹窗 (高斯模糊蒙层) ]
                  │
          [ 用户点击「立即登录」]
                  │
                  ▼
          [ 调用 Host 设备码登录流程 (获取 verification_url + user_code) ]
                  │
          [ 唤起系统默认浏览器打开授权页，弹窗切换为「轮询中态」]
                  │
         [ 轮询检测是否授权成功? ]
           ├── 成功 ──> 写入本地 Token -> 触发 resolveAll() -> 关闭弹窗 -> 自动恢复执行 onSuccess() (打开目标 Stage)
           └── 取消/超时 ──> 用户点击 ✕ 关闭 -> 触发 rejectAll() -> 关闭弹窗 -> 停留当前页
```

### 5.2 状态机定义
- `closed`：门禁关闭状态（默认）。
- `checking`：正在静默检查本地鉴权缓存。
- `starting`：用户点击登录，正在请求设备码与授权 URL。
- `waiting`：已获取设备码，正在前台展示并在后台轮询 Auth 状态。
- `authorized`：授权完成，分发 profile，执行意图恢复。
- `error` / `expired`：授权超时或网络异常，提供重试按钮。

---

## 6. Seam 跨插件接口契约

由 `plugins/omnimux` 执行中枢在客户端全局 `window` 下挂载单例对象。详细架构见 `docs/specs/2026-08-20-omnimux-login-gate.md`。本 PRD 只冻结产品面必须遵守的接口：

```typescript
interface OmnimuxAuthGate {
  /** 获取当前登录态（verify=false 时仅读内存/本地缓存） */
  getStatus(verify?: boolean): Promise<{ logged_in: boolean; profile?: any }>;

  /**
   * 核心登录守卫：未登录时弹窗并排队 intent，登录成功后自动执行 onSuccess
   */
  ensureLogin(options: {
    reason?: string;             // 触发来源，如 "sidebar:omnimux-workflow"
    onSuccess?: (profile: any) => void; // 登录成功后恢复执行的回调
    onCancel?: () => void;       // 用户主动关闭/放弃的回调
  }): void;

  /** 关闭弹窗并放弃所有挂起意图 */
  cancel(): void;

  /** 订阅门禁状态变更 (用于 React useSyncExternalStore 驱动 UI 渲染) */
  subscribe(listener: () => void): () => void;

  /** 获取门禁快照数据 */
  getSnapshot(): AuthGateState;
}
```

硬约束（与 AGENTS.md 对齐）：
- 登录/身份属 OmniMux 核心，全部改动落在 `plugins/omnimux`；垂直包只消费 seam，不实现登录 UI。
- 跨 bundle 不得 `import` hub client；垂直包通过 `window.__omnimuxAuth` 懒读取。
- 不做启动门禁；检测按需（点击时），不每次联网强制 verify。
- Agent 工具返回 `needs-omnimux` **不自动弹窗**，保持结构化错误（遵守 `docs/decisions/2026-08-14-execution-hub.md`）。

---

## 7. 异常边界与非功能性要求

1. **防抖与重复点击拦截（Debounce & Concurrency）**：
   - 当门禁已处于打开状态时，用户重复点击其它插件不会重复打开多个弹窗，而是将新的 `onSuccess` 追加到意图队列中（单门单例保证）。
2. **多端/多 Tab 状态同步**：
   - 授权成功后，通过 `window.postMessage` 或 Storage 事件广播登录态，刷新所有已挂载的 OmniMux Stage 组件。
3. **网络超时与断网降级**：
   - 设备码轮询默认最长持续 5 分钟，超时后展示「授权已过期，点击重新获取」，不发生前端崩溃。
4. **轻量与性能**：
   - 弹窗采用 `React Portal` 挂载至 `document.body`，未激活状态下 DOM 节点返回 `null`，零内存开销。
5. **视觉 Token**：
   - 生产组件 100% 消费 `--dsw-alias-*` / `--dsw-specific-*`；原型 hex 仅作视觉对照，不得直接进生产。

---

## 8. Non-goals

- 不做 App 启动全屏登录墙。
- 不在垂直插件内各自实现登录弹窗或品牌海报。
- 不把 Agent 工具层的 `needs-omnimux` 自动映射为弹窗。
- 不实现文案 A/B 在线实验（探索稿已废弃，不再上线切换器）。
- 不更换左侧深海配图、不更换官方章鱼 Logo。

---

## 9. 交付物清单

| 交付项 | 存储路径 / 形式 | 当前状态 | 责任人 |
|---|---|---|---|
| **1:1 高保真 HTML 原型** | `docs/prototypes/omnimux-login-gate-modal.html` | ✅ 已锁定视觉与默认文案 A | 齐活林 |
| **PRD 功能需求规格说明书** | `docs/specs/2026-08-28-omnimux-universal-login-gate-prd.md` | ✅ **accepted / 文案已锁定** | 齐活林 / 许清楚 |
| **架构与扩展点设计** | `docs/specs/2026-08-20-omnimux-login-gate.md` | 🔄 工程落地时对照 | 高见远 |
| **后续代码落地与组件化** | `plugins/omnimux/src/client/` | ⏳ 待老板下达工程启动指令 | 林深 / 严过关 |

---

## 附录 A. 已废弃的探索文案（禁止生产使用）

以下版本仅存在于原型调试栏，便于对照审阅。**工程实现、QA 验收、后续修订一律不得采用。**

| 版本 | 主标题 | 副标题 | 三条特性 | 处置 |
|---|---|---|---|---|
| B 全栈 AIGC 创作中枢 | OmniMux Studio 全面接入 AI 创作中枢 | 顶尖通用多模态模型 + 专业级协同工作流 | 感知编排全模态 / 无限画布资产库 / 一键成片 | 废弃 |
| C 1:1 句式对齐 | OmniMux 全面接入 AI Agent 矩阵 | 最强创作 Agent + 顶尖通用多模态模型 | 多场景多模态任务 / Skill 矩阵 / 一路出海出片 | 废弃 |
| MiniMax 原版对照 | MiniMax Design 全面接入 H3 | 最强创作 Agent + 顶尖通用多模态模型 | 多场景内容生产 / 工作流与 Skill / Design陪你落地 | 仅参考，禁止上线 |
| 早期探索稿 | OmniMux 全面接入 AI Agent 矩阵 | 最强多模态生成管线 + 顶尖多智能体协同生产力 | 跨场景商业创作 / Agent Skill 十倍内容 / 出海出片 | 已从正文清除 |

---

## 附录 B. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| 1.0.0 | 2026-08-28 | 初稿：1:1 复刻弹窗结构、Seam 与意图恢复 |
| 1.0.1 | 2026-08-28 | 锁定深海配图；重构左侧黄金比例排版；替换官方章鱼 Logo |
| 1.1.0 | 2026-08-29 | **老板拍板锁定「商业出海与出片」文案**；清除正文中的 A/B/C 并列与旧稿残留；文档状态改为 `accepted` |
